torch>=2.0.0
torchvision>=0.15.0
numpy>=1.22.0
pillow>=9.0.0
opencv-python>=4.5.0
matplotlib>=3.5.0
seaborn>=0.11.0
scikit-learn>=1.0.0
scipy>=1.8.0
flask>=2.0.0
flask-cors>=3.0.0
onnx>=1.12.0
onnxruntime>=1.12.0
tqdm>=4.64.0

# NOTE: TensorFlow Lite export is an OPTIONAL, heavier deployment target.
# It is intentionally kept out of this core file so the main training /
# evaluation / web app workflow stays lightweight and dependency-light.
# See requirements-tflite.txt to enable `src/export.py::export_to_tflite`.
